package chapter6.staticex;

public class Student {
	
	// 필드 선언 (멤버 변수)
	// [접근자] [static] 데이터타입 변수이름;
	
	// 정적 필드(static) or non-instance 필드 : 메모리가 static area mehod area
	static int serialNum;
	
	// 인스턴스 필드 : 메모리가 힙 영역
 	
	int studentId;
	int grade;
	String address;
	
}
